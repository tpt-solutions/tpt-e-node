//! tpt-node-esp32 — ESP32 HAL for TPT Embedded Node.
//!
//! Implements `tpt_e_link::LinkTransport` for UART and BLE using
//! `tpt-embedded-core` primitives. Not yet implemented — depends on
//! `tpt-embedded-core` which is in Phase 0 of its own roadmap.

#![cfg_attr(not(feature = "std"), no_std)]

pub fn placeholder() {}
